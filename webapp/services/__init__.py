"""UI-free service helpers for the web app (no nicegui imports).

These modules hold logic that is driven by the nicegui shell but never touches UI elements directly,
so they can be unit-tested without the NiceGUI runtime.
"""
